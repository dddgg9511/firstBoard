<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//En" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board.noticeBoard.repository.PostRepository">
	<select id="findAll" parameterType="Board.noticeBoard.domain.Post" resultType="Board.noticeBoard.domain.Post">
		SELECT 
			P.POST_ID,
			P.POST_TITLE,
			P.POST_AUTHOR,
			P.POST_CATEGORY
			P.POST_BODY,
			P.POST_DATETIME
		FROM POST
		ORDER BY POST.DATETIME DESC
	</select>
	
	<select id="findByID" parameterType="Long" resultType="Board.noticeBoard.domain.Post">
		SELECT 
			POST_ID,
			POST_TITLE,
			POST_AUTHOR,
			POST_CATEGORY
			POST_BODY,
			POST_DATETIME
		FROM POST
		WHERE POST_ID = #{postNumber} 
	</select>
	
	<insert id="save" parameterType="Long">
		INSERT INTO POST 
		(
			TITLE.
			AUTHOR,
			CATEGORY,
			BODY,
			DATETIME
		)
		VALUES
		(
			#{title},
			#{author},
			#{category},
			#{body},
			NOW()
		)
	</insert>
</mapper>